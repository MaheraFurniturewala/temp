import IconButton from "./icon-button"

type Position = {
    position: string
    company: string
    image: string
    from: string
    to: string
    description: string[]
    skills?: IconButton[]
}

export default Position
