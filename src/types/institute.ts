import IconButton from "./icon-button"

type Institute = {
    name: string
    degree: string
    image: string
    from: string
    to: string
    description: string[]
    skills: IconButton[]
}

export default Institute
